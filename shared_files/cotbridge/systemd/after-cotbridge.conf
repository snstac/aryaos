[Unit]
# COTBridge and GutCheck start before the clock gate so routed-MANET CoT can
# discover a time root. Downstream feeders wait for the bounded attempt, then
# start even if no trustworthy source was found.
After=cotbridge.service aryaos-time-ready.target
Wants=cotbridge.service aryaos-time-ready.target
