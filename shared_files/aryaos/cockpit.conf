# AryaOS Cockpit — reverse-proxy behind lighttpd (HTTPS on :443).
#
# cockpit-ws listens on loopback only (see systemd drop-in). lighttpd terminates
# TLS and proxies with X-Forwarded-* headers.
#
# SPDX-License-Identifier: Apache-2.0
# Copyright Sensors & Signals LLC https://www.snstac.com/

[WebService]
AllowUnencrypted = true
ProtocolHeader = X-Forwarded-Proto
ForwardedForHeader = X-Forwarded-For
UrlRoot = /admin
# Browsers send Origin https://<hostname-or-ip> when using the portal; fnmatch globs are allowed
# (cockpit.conf(5)). Appliance is reached by RFC1918 / mDNS names, not only localhost.
Origins = https://* wss://*
