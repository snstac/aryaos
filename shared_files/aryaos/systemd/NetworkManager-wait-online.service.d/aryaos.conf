[Service]
# AryaOS can be fully useful on its hotspot, PAN, or IPv4LL while an Ethernet
# profile continues looking for optional DHCP.  Wait for any usable NetworkManager
# connection instead of blocking startup on every autoconnect profile settling.
ExecStart=
ExecStart=/usr/bin/nm-online --quiet --timeout=60
