# Match the mode of Debian's packaged /etc/bluetooth directory. bluetoothd is
# still read-only there because the vendor unit has ProtectSystem=strict; this
# only prevents systemd from warning that its ConfigurationDirectoryMode=0555
# conflicts with the existing root-owned 0755 configuration directory.
[Service]
ConfigurationDirectoryMode=0755
