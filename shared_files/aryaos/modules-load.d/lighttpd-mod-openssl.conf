# Debian's lighttpd-mod-openssl package requests the optional kernel TLS module
# for kTLS acceleration. Raspberry Pi OS kernels used by AryaOS omit that module;
# HTTPS continues through user-space OpenSSL. This higher-priority /etc file
# overrides the same-named /usr/lib/modules-load.d file and avoids a failed load
# on every boot.
