/deploy/
/config.local
.DS_Store
/pi-gen/
.venv/
# MkDocs local HTML output (Read the Docs builds from sources)
/site/
# pi-gen Docker bind mounts (large; persist between retries)
.aryaos-pigen-work/
.aryaos-pigen-deploy/
__pycache__/
node_modules/

# Lab Pi SSH password / overrides (never commit)
scripts/.dev-pi-creds.local

# Ansible become password for dragonball (see playbooks/dragonball-secret.yml.example)
playbooks/dragonball-secret.yml

# Lab dev SSH private key (public key aryaos-dev-lab.pub is committed for image authorized_keys)
shared_files/aryaos/ssh/aryaos-dev-lab

# Offline docs built into the portal at image-build time (mkdocs output)
shared_files/aryaos/html/docs/
